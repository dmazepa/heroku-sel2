{
  "environments": {
    "test": {
      "buildpacks": [
        { "url": "heroku/google-chrome" },
        { "url": "heroku/python" }
      ],
      "scripts": {
        "install": "pip install robotframework",
        "test": "python main.py"
        //"test": "robot login_tests/valid_login.robot"
      }
    }
  }
}